attributes
	session : UserInfomation(CurrentUserInfomation)
		  ProjectItems(Iterator)

parameters :
	request : method(int) controller specific request parameter,used to differ the different requests for the same servlet.